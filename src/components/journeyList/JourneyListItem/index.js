import {Container} from "../journeyListElements";

const JourneyListItem = ({departureStationName, returnStationName, coveredDistance}) => {
    return (
        <Container>
            <div>l</div>
        </Container>
    );
};

export default JourneyListItem;